package fit.iuh.event_service.models.enums;

public enum EventStatus {
    DRAFT,
    PENDING, // Bản nháp
    PUBLISHED, // Đã công khai (Trong ảnh là PUBLISHER, mình chỉnh lại cho chuẩn)
    CANCELLED       // Đã hủy
}
