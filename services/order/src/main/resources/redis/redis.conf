# Redis config for local testing
# Enables key-expiration events (pub-sub) so Spring can listen and auto-expire orders.

bind 0.0.0.0
port 6379
protected-mode no

# Publish events for expired keys
notify-keyspace-events Ex
